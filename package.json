{
  "scripts": {
    "server": "live-server public --port-3000",
    "compile": "sass src/sass/index.scss:public/style/index.css",
    "clear": "rimraf public/style",
    "watch": "npm run compile -- --watch",
    "dev": "run-p server watch"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "sass": "^1.54.9"
  }
}
